error_level: "E_ALL"

class_name: IntegrationSuiteGuy

modules:
    enabled:
        - Nette
    config:
        Nette:
            tempDir: tests/_temp
            configFiles:
                - tests/integration/config.neon
            robotLoader:
                - tests/integration
